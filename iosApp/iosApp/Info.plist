<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <dict>
        <!-- Disables the frame rate limit on ProMotion displays -->
        <key>CADisableMinimumFrameDurationOnPhone</key>
        <true/>

        <!-- Describes why the app needs access to the user's photo library -->
        <key>NSPhotoLibraryUsageDescription</key>
        <string>This app needs access to your photos to select an image for background removal.</string>

        <!-- Add other keys your app might need, like CFBundleIdentifier, etc. -->
        <key>CFBundleIdentifier</key>
        <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
        <key>CFBundleVersion</key>
        <string>1</string>
        <key>CFBundleExecutable</key>
        <string>$(EXECUTABLE_NAME)</string>
        <key>CFBundleName</key>
        <string>$(PRODUCT_NAME)</string>
        <key>CFBundleShortVersionString</key>
        <string>1.0</string>

    </dict>
</plist>
